{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "sites/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "dependencies": {
    "eslint": "8.31.0",
    "eslint-config-next": "13.1.1",
    "turbo": "^1.7.0"
  }
}
